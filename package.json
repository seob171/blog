{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "web": "turbo --filter web",
    "add:blog": "pnpm add --filter blog",
    "blog": "turbo --filter blog",
    "server": "turbo --filter server",
    "oas": "turbo --filter oas-generator",
    "playground": "turbo --filter playground",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.4",
    "lint-staged": "^15.2.7",
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.0"
}
